apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: techchallenge2
  region: us-east-1

iam:
  serviceRoleARN: arn:aws:iam::158468816134:role/LabRole

managedNodeGroups:
  - name: standard-workers
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 1
    maxSize: 3
    volumeSize: 20
    iam:
      instanceRoleARN: arn:aws:iam::158468816134:role/LabRole
